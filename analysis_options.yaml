include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_single_quotes: true
    require_trailing_commas: true

analyzer:
  exclude:
    - '**/*.g.dart'
    - lib/main.data.dart
    # The bundled package's standalone, legacy demo is not part of this app.
    - packages/hijri_picker/example/**
